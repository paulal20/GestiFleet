<div class="reserva-detalle-content">

  <div id="alertasDetalle" class="container mt-3"></div>

  <div class="d-flex justify-content-between align-items-center mb-4">
    <h1><%= title %></h1>
    <% if (usuarioSesion && usuarioSesion.rol === 'Admin') { %>
      <a href="/reserva/listareservas" class="btn btn-outline-secondary">
        <span class="lang lang-es">Ver Todas las Reservas</span>
        <span class="lang lang-en d-none">View All Bookings</span>
      </a>
    <% } else { %>
      <a href="/reserva/mis-reservas" class="btn btn-outline-secondary">
        <span class="lang lang-es">Mis Reservas</span>
        <span class="lang lang-en d-none">My Bookings</span>
      </a>
    <% } %>
  </div>

  <div class="vehiculo-card card mx-auto" style="max-width: 900px;">
    <div class="row g-0">

      <div class="col-md-6">
        <div class="vehiculo-img-container h-100">
          <% if (reserva.tiene_imagen) { %>
            <img src="/vehiculos/<%= reserva.id_vehiculo %>/imagen" class="rounded-start img-cover" alt="<%= reserva.marca %> <%= reserva.modelo %>">
          <% } else { %>
            <div class="d-flex flex-column justify-content-center align-items-center">
              <img src="/bootstrap-icons-1.13.1/car-front-fill.svg" class="bi-svg" alt="">
              <span class="mt-2 fw-bold">Sin Imagen</span>
            </div>
          <% } %>
        </div>
      </div>

      <div class="col-md-6 d-flex flex-column">
        <div class="card-header">
          <h5 class="mb-0 fw-bold">
            <span class="lang lang-es">Detalles de la Reserva</span>
            <span class="lang lang-en d-none">Booking Details</span>
          </h5>
        </div>

        <div class="card-body flex-grow-1">

          <div class="row mb-2">
            <div class="col-sm-4 fw-bold">Vehículo</div>
            <div class="col-sm-8"><%= reserva.marca %> <%= reserva.modelo %> (<%= reserva.matricula %>)</div>
          </div>

          <div class="row mb-2">
            <div class="col-sm-4 fw-bold">Usuario</div>
            <div class="col-sm-8"><%= reserva.nombre_usuario %> (<%= reserva.email_usuario %>)</div>
          </div>
          
          <hr class="my-3">

          <div class="row mb-2">
            <div class="col-sm-4 fw-bold">Inicio</div>
            <div class="col-sm-8"><%= new Date(reserva.fecha_inicio).toLocaleString() %></div>
          </div>
          <div class="row mb-2">
            <div class="col-sm-4 fw-bold">Fin</div>
            <div class="col-sm-8"><%= new Date(reserva.fecha_fin).toLocaleString() %></div>
          </div>
          
          <hr class="my-3">

          <div class="row mb-2">
            <div class="col-sm-4 fw-bold">Estado</div>
            <div class="col-sm-8">
              <% if (reserva.estado === 'activa') { %>
                <span id="badgeEstado" class="badge bg-success">Activa</span>
              <% } else if (reserva.estado === 'finalizada') { %>
                <span id="badgeEstado" class="badge bg-secondary">Finalizada</span>
              <% } else if (reserva.estado === 'cancelada') { %>
                <span id="badgeEstado" class="badge bg-danger">Cancelada</span>
              <% } %>
            </div>
          </div>

        </div> 

        <div class="perfil-actions d-flex" id="contenedorBotones">
          <% if (reserva.estado === 'activa') { %>
            <button type="button" 
                    id="btnAbrirModal"
                    class="btn btn-primary w-100" 
                    data-bs-toggle="modal" 
                    data-bs-target="#modalCancelarDetalle"
                    data-id="<%= reserva.id_reserva %>">
              <span class="lang lang-es">Cancelar Reserva</span>
              <span class="lang lang-en d-none">Cancel Booking</span>
            </button>
          <% } else { %>
             <p class="text-muted text-center w-100 mb-0">
                <span class="lang lang-es">Esta reserva no se puede modificar.</span>
                <span class="lang lang-en d-none">This booking cannot be modified.</span>
             </p>
          <% } %>
        </div>

      </div>
    </div> 
  </div> 
</div>

<div class="modal fade" id="modalCancelarDetalle" tabindex="-1" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title">Confirmar Cancelación</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body">
        ¿Estás seguro de que deseas cancelar esta reserva? Esta acción es irreversible.
        <div class="d-flex justify-content-end mt-3 gap-2">
            <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cerrar</button>
            <button type="button" id="btnConfirmarCancelar" class="btn btn-primary">Confirmar</button>
        </div>
      </div>
    </div>
  </div>
</div>

<script src="/js/jquery-3.7.1.min.js"></script>
<script src="/js/ajax/reservas/reservaDetalle.js"></script>